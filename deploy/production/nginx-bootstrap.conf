# Enable this HTTP-only site BEFORE obtaining the first public certificate.
server {
    listen 80;
    server_name api.deepecho.top chat.deepecho.top;
    location ^~ /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 404; }
}
