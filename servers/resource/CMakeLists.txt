find_package(PNG REQUIRED)
find_package(libjpeg-turbo CONFIG REQUIRED)
add_executable(resource_server main.cpp ResourceService.cpp Avatar.cpp
    ../chat/config/ConfigMgr.cpp ../chat/files/EncryptedFileStore.cpp)
target_include_directories(resource_server PRIVATE . ../chat/config ../chat/files "${PROJECT_SOURCE_DIR}/common")
target_compile_definitions(resource_server PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
target_link_libraries(resource_server PRIVATE chat_server_dependencies chat_security chat_observability PNG::PNG $<IF:$<TARGET_EXISTS:libjpeg-turbo::turbojpeg>,libjpeg-turbo::turbojpeg,libjpeg-turbo::turbojpeg-static>)
chat_copy_runtime_config(resource_server "resource1.ini" "resource1.ini")
chat_copy_runtime_config(resource_server "resource2.ini" "resource2.ini")
if(BUILD_TESTING)
    add_executable(resource_store_test StoreTest.cpp ../chat/files/EncryptedFileStore.cpp)
    target_include_directories(resource_store_test PRIVATE ../chat/files "${PROJECT_SOURCE_DIR}/common")
    target_compile_definitions(resource_store_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    target_link_libraries(resource_store_test PRIVATE chat_security Threads::Threads)
    add_test(NAME resource_store_recovery COMMAND resource_store_test "${CMAKE_CURRENT_BINARY_DIR}/store-test")
    set_tests_properties(resource_store_recovery PROPERTIES TIMEOUT 30)
endif()
