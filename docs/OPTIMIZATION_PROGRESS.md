# 优化实施记录

本轮范围：消息可靠性、业务并发、可观测性、压测、故障验证和部署说明。
保留开始本轮时已有的工作树修改；不把已有功能算作本轮新增成果。

## 验收清单

- [x] 有界消息窗口、ACK 后续投、在线重试、数据库失败不误报 ACK 成功
- [x] 有界会话分片执行、异步跨节点通知、队列与依赖等待指标
- [x] 压测即时票据、超时、正确统计口径和等价日志微基准
- [ ] 大量积压、丢 ACK、依赖中断、Chat 节点终止后的真实状态核对（脚本与 CI 已就绪；本机未安装 Docker）
- [ ] PR 双节点门禁已配置；固定资源性能验收、多机存储与恢复仍需补齐
- [x] 客户端 SQLite 收发持久化、重启恢复与稳定消息 ID 重试（本机存储回归通过）
- [x] 连接字节预算、聊天/文件独立速率限制、文件工作队列字节预算
- [x] 通知队列停机取消、全局指标去重、失败性能结果拒绝比较
- [x] 文本消息服务依赖拆分及失败注入回归
- [x] 附件不可变分片、重复确认、落盘同步与截断记录拒绝回归
- [ ] 其他业务服务依赖拆分、开放到达率压测与真实多机/掉电验收
- [x] 编译与回归验证；未执行的集成/性能项目明确说明原因

运行结果在完成相应验证后追加，不预先填写 PASS。

## 2026-09-06 本机验证

- Windows Release 构建：PASS。
- 服务端 CTest：15/15 PASS；Qt 客户端 CTest：3/3 PASS，包含 SQLite 重启恢复与 UI smoke。
- 压测协议测试：4/4 PASS，覆盖登录超时、读超时、响应拒绝、响应 ID 不匹配和未消费帧保留。
- Python 语法检查：PASS，5 个脚本。
- Docker 双节点 E2E、故障恢复和真实性能采样：NOT RUN，本机 Docker 命令不可用，WSL 列举因权限被拒绝；CI 的 `docker-e2e` job 已配置这些场景，尚未取得运行结果。

## 2026-09-07 续补验证

- Windows Release 构建：PASS；服务端 CTest：16/16 PASS。
- 新增 TextMessageService 回归：非好友、查询不可用、提交失败、依赖异常、身份不匹配与超长内容。
- 扩展附件回归：同偏移重复写不截断后续分片、内容冲突拒绝、已有文件拒绝覆盖、截断记录不填零恢复。
- 本轮 C++ 修改的 clang-format 检查与工作树 diff 空白检查：PASS。
- Qt 客户端未在本轮修改，沿用上一轮 3/3 PASS；未重复执行。
- 多机存储方案及恢复限制见 [FILE_RECOVERY.md](FILE_RECOVERY.md)。真实双节点、跨主机与掉电测试仍未执行。
