# 参与贡献

提交修改前请阅读 [开发指南](docs/DEVELOPMENT.md) 和 [构建说明](docs/guides/build.md)。

## 问题反馈

请说明操作系统、编译器或 Qt 版本、当前提交、复现步骤、预期行为和实际行为。日志应去除密码、验证码、令牌、邮件地址及聊天内容。

## 提交修改

1. 从独立分支开始，一次修改聚焦一个问题。
2. 沿用现有目录职责和 `.clang-format` 风格。修改协议、配置或数据库结构时，同步更新示例与迁移说明。
3. 执行与变更相关的构建和测试，在 Pull Request 中记录命令、结果以及未能验证的部分。
4. 说明解决的问题和修改后的行为；界面变化附上截图，错误修复附上复现步骤。

不要提交 `build/`、Python 缓存、`node_modules/`、个人 CMake 预设、日志或真实凭据。第三方代码通过现有子模块管理。

文档按主题归入 `docs/guides/` 或 `docs/deployment/`，并更新 [文档导航](docs/README.md)。根 README 用于项目介绍和入门，详细操作步骤放在对应指南中。
